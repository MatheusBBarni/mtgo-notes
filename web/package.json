{
  "name": "mtgo-notes-web",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "build": "astro build",
    "test:build": "astro build && vitest run --config vitest.build.config.ts"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "astro": "^7.2.2"
  }
}
