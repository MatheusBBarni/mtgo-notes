# Frozen Tauri host

This folder is the previous Tauri 2 / React / Rust companion. It is not built
by the default CI or solution.

To work on it, `cd` here and use the old `npm ci` / `npm run verify` flow.
The active app is the WinUI 3 host at the repository root.
